---
meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: security2-0-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 80
